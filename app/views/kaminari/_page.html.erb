<% if page.current? %>
  <span class="bg-green-500 text-white py-2 px-4 rounded shadow"><%= page %></span>
<% else %>
  <%= link_to page, url, remote: remote, rel: page.rel, class: "text-gray-800 font-light hover:bg-gray-200 py-2 px-4 rounded shadow" %>
<% end %>
